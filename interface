from pygame_functions import *
import random

screenSize(800 , 800)

#Demostración de como funcionan los inputs en python, con pygame_functions

instructionLabel = makeLabel("Please enter a word", 40, 10, 10, "blue", "Agency FB", "yellow")
showLabel (instructionLabel)

#posición en x, posición en y, Ancho, Altura (predeterminada 0), (Permitir la entrada de cualquier caracter=0, solo minúsculas=1, sólo mayúsculas=2), texto por defecto, número máximo de caracteres (si es 0 no tiene limites), tamaño de la fuente
wordBox= makeTextBox(10, 80, 300, 0, "Enter text here", 0, 24)

#Mostrar la caja de texto
showTextBox(wordBox)

#Como obtener un formato de entrada, variable en la que se guarda lo que ingresa el usuario
entry = textBoxInput(wordBox)

#variable, tamaño de fuente, posición x, posición y, rojo

wordLabel = makeLabel(entry, 30, 100, 300,"red")
showLabel(wordLabel)

"""Otras funciones útiles
pause(1000) - pausa en milisegundos
hideLabel (x) - econder un objeto que se había mostrado
No hacer labels con el mismo nombre
"""

endWait()

